# Release smoke-test input

`tutorial_samples.xlsx` is an Excel copy of
`Tutorial/dataset/ForamEcoQS_Tutorial_Dataset.csv`, with numeric abundance cells.
The manual build workflow uses it to run a complete CLI calculation and Excel
export from each published platform package with the bundled Jorissen databank.
It contains synthetic tutorial data, not new scientific validation data.
